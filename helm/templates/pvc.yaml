apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "webhook.fullname" . }}-data
  labels: {{ include "webhook.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
